apiVersion: v1
kind: Service

metadata:
  name: brain-task-service
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-scheme: internet-facing

spec:
  type: LoadBalancer

  selector:
    app: brain-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80